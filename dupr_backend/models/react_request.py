# coding: utf-8

"""
    DUPR Backend APIs

    Internal RESTful APIs to access DUPR ratings, users and provide matches.

    The version of the OpenAPI document: v1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self

class ReactRequest(BaseModel):
    """
    ReactRequest
    """ # noqa: E501
    actor: StrictInt = Field(description="Obfuscated Id of user do the reaction")
    id: StrictStr = Field(description="The post's id")
    react: StrictStr = Field(description="type of reaction, if COMMENT then add the contain in the 'comment', else let it empty")
    comment: StrictStr = Field(description="If the react type is comment, then put the contain here")
    tags: List[StrictInt]
    images: List[StrictStr]
    matches: List[StrictInt]
    react_on: StrictStr = Field(description="if react on comment, add this comment getstreamId here, if not lets empty", alias="reactOn")
    __properties: ClassVar[List[str]] = ["actor", "id", "react", "comment", "tags", "images", "matches", "reactOn"]

    @field_validator('react')
    def react_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['LIKE', 'COMMENT']):
            raise ValueError("must be one of enum values ('LIKE', 'COMMENT')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ReactRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ReactRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "actor": obj.get("actor"),
            "id": obj.get("id"),
            "react": obj.get("react"),
            "comment": obj.get("comment"),
            "tags": obj.get("tags"),
            "images": obj.get("images"),
            "matches": obj.get("matches"),
            "reactOn": obj.get("reactOn")
        })
        return _obj


